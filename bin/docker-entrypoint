#!/bin/bash
set -e

# If running the rails server, use the PORT environment variable if set
if [ "${1}" == "./bin/rails" ] && [ "${2}" == "server" ]; then
  if [ -n "${PORT:-}" ]; then
    exec "${@}" -p "${PORT}"
  else
    exec "${@}" -p 8080
  fi
else
  exec "${@}"
fi